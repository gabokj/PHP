<?php

// operador and &&

$empregado = true;
$homeOficce = true;

var_dump( value: $empregado && $homeOficce);

// Operador OR ||
$empregado = true;
$homeOficce = false;

var_dump( value: $empregado || $homeOficce);

// Operador OR exclusivo XOR ^
$empregado = true;
$homeOficce = false;

var_dump( value: $empregado ^ $homeOficce);

// Operador "Não" !
$empregado = true;

var_dump(!$empregado);

